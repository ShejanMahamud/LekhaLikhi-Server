{
  "version": 2,
  "builds": [
    {
      "src": "/dist/app/app.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb",
        "timeout": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/app/app.js",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
    }
  ]
}
